"""Skeleton test module to ensure pytest always has at least one test."""


def test_placeholder():
    """Placeholder test that always passes.
    
    This ensures the pytest workflow always has at least one test to run,
    preventing pytest from exiting with code 5 (no tests found).
    """
    assert True
